import {StyleSheet} from "react-native";

export const styles = StyleSheet.create({
    container: {
        marginTop: 10,
        flexDirection: 'row',
    },
    logo: {
        width: 60,
        height: 30,
    },
    backButton: {
        flex: 1
    }
});