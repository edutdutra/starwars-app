import {StyleSheet} from "react-native";

export const styles = StyleSheet.create({
    container: {
        backgroundColor: "#DDDEDF",
        flex: 1,
        padding: 24,
        alignItems: 'center',
    },
    logo: {
        width: 300,
        height: 120,
        marginTop: 10
    },
    menu: {
        marginTop: 180
    }
});